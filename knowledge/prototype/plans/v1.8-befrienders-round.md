---
title: "Plan — v1.8 the befrienders' round"
status: executed
last_updated: 2026-09-20
based_on: [prototype/lions-befrienders-2026-09-11-feedback, prototype/roadmap]
---

# v1.8 — the small half of the Lions Befrienders list

**Goal:** fix what the 11 Sept session found that can be fixed inside the current data
model, in one round, each item behind a test. The big half (many seniors per caregiver,
what each side sees of the other, a danger alert) goes to the roadmap, not this plan.

**Spec:** `app/SPEC.md` §2 boundaries, §9.5 hard rules. Register:
[[../lions-befrienders-2026-09-11-feedback]].

## Tasks, as executed

1. **Service-mismatch guard** (L9.1). `matching.assign` raises `ServiceMismatch` unless
   `confirm_mismatch`; `best_available` requires `service_ok`; `POST /admin/visits/{id}/assign`
   returns 409 `service_mismatch`; the console row says "⚠ does not offer X" and the
   confirm dialog changes; a confirmed override is one audit row,
   `visit_assigned_service_mismatch`, written after the update. Smoke: 409 without the flag,
   200 with it, audited; sweep leaves a non-offered service unmatched. e2e: dismiss, then
   accept, then the API path.
2. **Urgent bookings with a duration** (L10.3). `VisitIn.hours` 1–8 in half-hour steps,
   urgent/soon only, ignored for planned; chips 1, 1.5, 2, 3, 4, 5, 6, 8 on the details
   step; priced on it. Smoke and e2e.
3. **Task notes and the family's note** (L9.5, L3.6). Four notes in `assumptions.json`
   rewritten as a message to a person; zh table the same; `visit.notes` appended to the
   kaki's assignment message, trimmed at 90 with an ellipsis, escaped in email. Smoke.
4. **Availability one door, plain words** (L9.2, L9.3). Profile row "When I'm free · Edit";
   screen title the same; "This is all you need to fill in"; exceptions block "Different on
   a date?" with a hint, chips Day off / Extra day. e2e.
5. **Gender two chips** (L9.4). e2e.
6. **Plainer screens** (L10.13, L1.6). Sign-in subtitle, role chips, "Who are you caring
   for?", "Kaki arrived", cost stack behind Show cost, care plan skippable on first setup,
   tier off the kaki profile, help quick questions from the room, guide answers for the
   door codes / how long / who to call, en + zh. e2e; LEAKS regex refreshed.
7. **Sender ID** (L9.6). Not code: noted in `deploy/EC2-DEPLOYMENT.md` and the roadmap.

## Verification

Smoke 394 → 415. Playwright 32 → 37 (`tests/e2e/v18.spec.js`). Second boot OK. One
review pass; its nine findings fixed in ea3306c (audit row placement, `hours` gate on
tier, zh guide order, note trimming, tier badge, half-hour chips, leftover `assign()`,
LEAKS regex, zh rewordings). Deployed 2026-09-20 with backup
`kakis.duckdb.bak-20260920-142734`; `assumptions.json` notes patched on the box rather than
overwritten, so the coordinator's rate edits stay. Production check without signing in:
`?v=1.8` assets, new subtitle, new role chips, new help questions, guide answer for "the
codes at the door".

*Connects to:* [[../roadmap]] · [[../feature-buckets-2026-09-04]] · [[../../journal/2026-09-20-v1.8-build]]
